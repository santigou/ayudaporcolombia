# Base URL pública de la API para los ejemplos copiables de la guía de partners
# (/partners/guia). VACÍA = usar el origen actual del navegador (funciona en
# dev gracias al proxy de Vite y en prod porque front y API comparten dominio).
# Ponla solo si la API vive en otro dominio: p. ej. VITE_API_URL=https://ayuda.tudominio.com
VITE_API_URL=