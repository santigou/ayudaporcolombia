{
  "name": "ayudaporcolombia",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server-nestjs",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run start:dev -w server-nestjs\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w server-nestjs",
    "start": "npm run start:prod -w server-nestjs",
    "prisma:migrate": "npm run prisma:migrate -w server-nestjs",
    "seed": "npm run seed -w server-nestjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
